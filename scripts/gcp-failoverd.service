[Unit]
Description=GCP Failoverd Agent
After=nginx.service

[Service]
Type=simple
ExecStart=/usr/bin/gcp-failoverd.sh #VIP
TimeoutStartSec=0
ExecStop=/bin/kill -2 $MAINPID
Restart=on-abort

[Install]
WantedBy=multi-user.target
